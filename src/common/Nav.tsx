import './Nav.css';
import React from 'react';

export default function Nav(props: INavProps) {
  const {
    
  } = props;

  return (
    <div>Nav</div>
  );
}

interface INavProps {
  [propsName: string]: any
}